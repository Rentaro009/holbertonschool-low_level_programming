#include <stdio.h>

/**
 * main - prints all single digits numbers of base 10
 *
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	printf("%d \n", n);
	return (0);
}
